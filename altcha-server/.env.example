# Altcha Server Configuration

# HMAC secret for token signing (leave empty for random generation)
ALTCHA_SECRET=

# Default PoW difficulty level (number of leading zero hex digits)
ALTCHA_DIFFICULTY_DEFAULT=2

# CORS origin restriction (empty to allow all origins)
ALTCHA_CORS_ORIGIN=
